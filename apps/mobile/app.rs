pub struct MobileApp;

impl MobileApp {
    pub fn new() -> Self {
        Self
    }

    pub fn run(&self) {
        println!("📱 Mobile Vault App (future stub)");

        println!("Planned features:");
        println!("- Biometric unlock");
        println!("- Encrypted vault sync");
        println!("- Secure sharing links");
        println!("- Offline-first storage");
    }
}
